@using OrchardCore.Commerce.ProductAttributeValues
@{
    TagBuilder tag = Tag(Model, "li");
    var attr = Model.Attribute as NumericProductAttributeValue;
    var label = Model.Label as string;
    tag.RenderStartTag();
    @T["{0}: {1}", label, attr.Value]
    tag.RenderEndTag();
}